# Legacy pre-Nexus EnvSpec path (historical 41690 lineage)

This directory contains files that previously lived in the active root tree while `main` documented the older Material Response 1.45 identity `41690c...`.

They are retained for provenance and historical review only.

They are **not** part of the current exact Nexus shipping path `e44183...`, which intentionally disables PTDE EnvSpec/PackedGI replacement.

Do not move these files back into the active release path unless a future source-complete implementation explicitly reintroduces the operator with its own verified routing, source and build lineage.
