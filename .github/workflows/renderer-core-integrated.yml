name: Renderer Core Integrated Single Addon

on:
  push:
    branches:
      - main
      - dev/renderer-core-v1-islands
      - agent/integrated-single-addon-a1-20260924
    paths:
      - "renderer-core/integrated/**"
      - "renderer-core/include/dsrrl/runtime/**"
      - "renderer-core/src/runtime/**"
      - "renderer-core/tests/integrated_feature_policy_tests.cpp"
      - "renderer-core/tools/audit_a1_protected_host_scope.py"
      - "renderer-core/data/a1_island_plan_index_v1.json"
      - "renderer-core/include/dsrrl/operators/**"
      - "renderer-core/src/operators/**"
      - "renderer-core/src/feature_registry.cpp"
      - "runtime-v1/CMakeLists.txt"
      - "runtime-v1/include/dsrrl/runtime/**"
      - "runtime-v1/src/**"
      - "runtime-v1/cmake/**"
      - "runtime-v1/tools/**"
      - "runtime-v1/provenance/**"
      - ".github/workflows/renderer-core-integrated.yml"
  pull_request:
    paths:
      - "renderer-core/integrated/**"
      - "renderer-core/include/dsrrl/runtime/**"
      - "renderer-core/src/runtime/**"
      - "renderer-core/tests/integrated_feature_policy_tests.cpp"
      - "renderer-core/include/dsrrl/operators/**"
      - "renderer-core/src/operators/**"
      - "runtime-v1/CMakeLists.txt"
      - "runtime-v1/include/dsrrl/runtime/**"
      - "runtime-v1/src/**"
      - ".github/workflows/renderer-core-integrated.yml"

permissions:
  contents: read

jobs:
  windows-integrated-build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Checkout pinned ReShade API
        uses: actions/checkout@v4
        with:
          repository: crosire/reshade
          ref: 3645e3025d1d98a90e318278858931f034d5d1f6
          path: _reshade

      - name: Audit protected shared-host scope
        shell: bash
        run: |
          python3 renderer-core/tools/audit_a1_protected_host_scope.py \
            --index renderer-core/data/a1_island_plan_index_v1.json

      - name: Configure integrated addon
        shell: pwsh
        run: >
          cmake
          -S renderer-core/integrated
          -B build-integrated
          -A x64
          -DRESHADE_INCLUDE_DIR="${{ github.workspace }}/_reshade/include"

      - name: Build integrated addon
        run: cmake --build build-integrated --config Release

      - name: Build and run integrated feature policy test
        shell: pwsh
        run: |
          cmake --build build-integrated --config Release --target dsrrl_renderer_integrated_policy_tests
          & "build-integrated/renderer-core-lib/Release/dsrrl_renderer_integrated_policy_tests.exe"
          if ($LASTEXITCODE -ne 0) { throw "Integrated feature policy test failed" }

      - name: Audit integrated operator boundary
        shell: pwsh
        run: |
          $entry = Get-Content renderer-core/integrated/integrated_addon.cpp -Raw
          $policy = Get-Content renderer-core/include/dsrrl/runtime/integrated_feature_policy.hpp -Raw

          foreach ($required in @(
            'engine::verify_provenance',
            'engine::install',
            'assets::register_runtime',
            'mr::register_runtime',
            'integrated_feature_policy'
          )) {
            if ($entry -notmatch [regex]::Escape($required)) {
              throw "Integrated entrypoint missing required glue: $required"
            }
          }

          foreach ($forbidden in @(
            'upper_lower::register_runtime',
            'envspec::register_runtime'
          )) {
            if ($entry -match [regex]::Escape($forbidden)) {
              throw "Deferred operator accidentally activated by integrated entrypoint: $forbidden"
            }
          }

          if ($policy -notmatch 'integrated_boot_enabled_count\(\) == 10u') {
            throw "Integrated policy boot-count invariant missing"
          }

          Write-Host "INTEGRATED_A2_OPERATOR_BOUNDARY_PASS"

      - name: Build audit and package
        shell: pwsh
        run: |
          $addon = "build-integrated/Release/DSRRL_Renderer_Core_Integrated_Construction.addon64"
          if (!(Test-Path $addon)) {
            throw "Integrated addon not produced"
          }

          $addonHash = (Get-FileHash $addon -Algorithm SHA256).Hash.ToLowerInvariant()
          $entryHash = (Get-FileHash "renderer-core/integrated/integrated_addon.cpp" -Algorithm SHA256).Hash.ToLowerInvariant()
          $policyHash = (Get-FileHash "renderer-core/include/dsrrl/runtime/integrated_feature_policy.hpp" -Algorithm SHA256).Hash.ToLowerInvariant()
          $a1Hash = (Get-FileHash "renderer-core/src/runtime/a1_create_pipeline_bridge.cpp" -Algorithm SHA256).Hash.ToLowerInvariant()
          $mrHash = (Get-FileHash "runtime-v1/src/mr_island.cpp" -Algorithm SHA256).Hash.ToLowerInvariant()
          $assetHash = (Get-FileHash "runtime-v1/src/asset_bridges.cpp" -Algorithm SHA256).Hash.ToLowerInvariant()
          $engineHash = (Get-FileHash "runtime-v1/src/engine_hooks.cpp" -Algorithm SHA256).Hash.ToLowerInvariant()

          $audit = [ordered]@{
            schema = "DSRRL_RENDERER_CORE_INTEGRATED_BUILD_AUDIT_V2"
            build_kind = "DEVELOPMENT"
            source_commit = $env:GITHUB_SHA
            reshade_commit = "3645e3025d1d98a90e318278858931f034d5d1f6"
            reshade_api = 20
            target = "DSRRL_Renderer_Core_Integrated_Construction.addon64"
            addon_sha256 = $addonHash
            entrypoint_sha256 = $entryHash
            integrated_policy_sha256 = $policyHash
            a1_bridge_source_sha256 = $a1Hash
            material_response_glue_sha256 = $mrHash
            asset_bridge_glue_sha256 = $assetHash
            engine_bridge_sha256 = $engineHash
            boot_armed_operator_count = 10
            single_addon_entrypoint = $true
            engine_hook_owner_count = 1
            deferred_upper_lower = $true
            deferred_legacy_envspec = $true
            deferred_full_pointlight = $true
            deferred_pmetal_source = $true
            runtime_status = "NOT_TESTED"
            bridge_activation = "NOT_TESTED"
            pixel_status = "OPEN"
          }

          New-Item -ItemType Directory -Force -Path package-integrated | Out-Null
          Copy-Item $addon package-integrated/
          Copy-Item renderer-core/integrated/README.md package-integrated/
          $audit | ConvertTo-Json -Depth 4 | Set-Content package-integrated/BUILD_AUDIT.json -Encoding UTF8

          Compress-Archive -Path package-integrated/* -DestinationPath DSRRL_Renderer_Core_Integrated_Construction.zip -Force
          Get-FileHash DSRRL_Renderer_Core_Integrated_Construction.zip -Algorithm SHA256

      - name: Upload integrated construction package
        uses: actions/upload-artifact@v4
        with:
          name: DSRRL_Renderer_Core_Integrated_Construction
          path: DSRRL_Renderer_Core_Integrated_Construction.zip
          if-no-files-found: error
